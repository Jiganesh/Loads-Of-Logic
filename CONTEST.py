import heapq
import bisect
import operator
from collections import deque
from itertools import accumulate
from typing import List




# def oddXor (N , array , Q, left, right):
    
#     def bit_count  (i , j):
#         return bin(prefixSum[j] ^ prefixSum[i-1])[2:].count('1')

    
#     prefixSum = [0]+list(accumulate(array, operator.xor))
#     query = tuple(zip(left, right))
    
#     r
        
                        
    


# # oddXor(3, [2,3,4], 1, [1], [3])
# oddXor(3, [2,3,5], 2, [1,2], [3,2])
# # oddXor(3, [2,3,5], 2, [1,1], [3,1])
